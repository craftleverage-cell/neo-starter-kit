# Category 3: MCP Enhancement Template

## Overview

This skill provides a higher-level workflow for the [MCP Server Name] MCP, making it easier to [achieve a specific outcome]. It encodes our team's best practices for using these tools.

## Key Workflow: [Name of Workflow]

This workflow simplifies the process of [user's goal] by orchestrating several MCP tools.

### Step 1: [Action involving first MCP tool]
- **Tool:** `[mcp_server].[tool_name]`
- **Purpose:** [Why this tool is used first].

### Step 2: [Action involving second MCP tool]
- **Tool:** `[mcp_server].[another_tool_name]`
- **Purpose:** [How this tool uses the output of the previous step].

## Domain-Specific Logic

- **Before calling any tools:** This skill first [performs a check or analysis, e.g., validates input against a schema in `references/schema.json`].
- **Best Practice:** When creating [a resource], always [a specific best practice, e.g., apply a specific tag or label]. This skill handles that automatically.
